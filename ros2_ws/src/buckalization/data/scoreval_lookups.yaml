relative_dist_limits : [3, 2, 8] # (ideal, min, max) in meters. Ideal is wherever it does detections best.
min_confidence : [0.3] # min confidence
max_id_dist : [2] #maximum distance in meters that the transform correction is allowed to be
# expected (width, height, min width, max width, min height, max height) in meters of buckets:
short_bucket : [0.3, 0.37, 0.2, 0.4, 0.25, 0.5]
tall_bucket : [0.3, 0.74, 0.2, 0.4, 0.6, 1]
max_intrabucket_dist_diff : [.5] # specifically for if 2 red buckets are measured to be significantly closer together/further apart