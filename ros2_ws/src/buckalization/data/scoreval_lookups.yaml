relative_dist_limits : [3, 2, 8] # (ideal, min, max) in meters. Ideal is wherever it does detections best.
min_confidence : 0.15 # min confidence
max_id_dist : 3.0 #maximum distance in meters that the transform correction is allowed to be
# expected (width, height, min width, max width, min height, max height) in meters of buckets:
short_bucket : [0.31, 0.37, 0.25, 0.35, 0.3, 0.45]
tall_bucket : [0.31, 0.74, 0.25, 0.35, 0.65, 0.82]
max_intrabucket_dist_diff : 0.5 # specifically for if 2 red buckets are measured to be significantly closer together/further apart
max_translation : 4.0 # maximum distance (m) change that can be published